// Template: Feature helpers
// Location: src/features/<feature>/helpers/feature-name.helpers.ts
// Rules: pure functions only — no React, no side effects, no API calls

import { FeatureItem } from '../types/feature-name.types'

/**
 * Format a feature item for display.
 * Replace with your actual helper logic.
 */
export function formatFeatureItem(item: FeatureItem): string {
  return item.id // TODO: implement
}

/**
 * Filter items by some criteria.
 * Replace with your actual helper logic.
 */
export function filterFeatureItems(items: FeatureItem[], query: string): FeatureItem[] {
  return items // TODO: implement
}
